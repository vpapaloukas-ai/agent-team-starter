# Keep shell scripts LF on every platform so the hooks/scripts run after a clone.
# (A Windows checkout with core.autocrlf=true would otherwise convert them to CRLF,
#  and bash then dies with: $'\r': command not found.)
*.sh text eol=lf
